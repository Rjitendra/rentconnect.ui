import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Introduction" />

# RentConnect UI Storybook

This workspace Storybook now covers:

- shared library components under `projects/shared/src/lib/components`
- reusable app-side components under `src/app/common/components`

Use the component stories to verify layout, inputs, and interaction states before wiring them into feature screens.
